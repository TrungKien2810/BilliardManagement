<UserControl x:Class="Billiards.UI.Views.PricingManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column: DataGrid -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0"
                    Background="{StaticResource PrimaryColor}"
                    CornerRadius="4"
                    Padding="12,8"
                    Margin="0,0,0,15">
                <TextBlock Text="Danh sách Quy tắc Giá theo Giờ" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Left"/>
            </Border>
            <DataGrid x:Name="dgPricingRules" 
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      SelectionChanged="dgPricingRules_SelectionChanged"
                      IsReadOnly="True"
                      VerticalAlignment="Stretch">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                    <DataGridTextColumn Header="Loại bàn" Binding="{Binding TableType.TypeName}" Width="*"/>
                    <DataGridTextColumn Header="Giờ bắt đầu" Binding="{Binding StartTimeSlot, StringFormat='{}{0:hh\\:mm}'}" Width="120"/>
                    <DataGridTextColumn Header="Giờ kết thúc" Binding="{Binding EndTimeSlot, StringFormat='{}{0:hh\\:mm}'}" Width="120"/>
                    <DataGridTextColumn Header="Giá/giờ" Binding="{Binding PricePerHour, StringFormat='{}{0:N0} VNĐ'}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Right Column: Form -->
        <StackPanel Grid.Column="1">
            <Border Background="{StaticResource PrimaryColor}"
                    CornerRadius="4"
                    Padding="12,8"
                    Margin="0,0,0,15">
                <TextBlock Text="Thông tin Quy tắc Giá" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Left"/>
            </Border>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Loại bàn:" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbTableType" 
                              DisplayMemberPath="TypeName" 
                              SelectedValuePath="ID"
                              Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBlock Text="Giờ bắt đầu (HH:mm):" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtStartTime" Height="30" Text="08:00"/>
                    <TextBlock Text="Ví dụ: 08:00, 22:00" FontSize="10" Foreground="Gray" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,10">
                    <TextBlock Text="Giờ kết thúc (HH:mm):" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtEndTime" Height="30" Text="22:00"/>
                    <TextBlock Text="Ví dụ: 22:00, 23:59" FontSize="10" Foreground="Gray" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Margin="0,0,0,20">
                    <TextBlock Text="Giá mỗi giờ (VNĐ):" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtPricePerHour" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnAddNew" 
                            Content="Làm mới" 
                            Width="110" 
                            Height="38" 
                            Margin="5,0" 
                            Click="btnAddNew_Click" 
                            FontSize="14"
                            ToolTip="Tạo quy tắc giá mới"/>
                    <Button x:Name="btnSave" 
                            Content="Lưu mới" 
                            Width="110" 
                            Height="38" 
                            Margin="5,0" 
                            Click="btnSave_Click" 
                            FontSize="14"
                            ToolTip="Lưu thông tin quy tắc giá"/>
                    <Button x:Name="btnDelete" 
                            Content="Xóa" 
                            Width="110" 
                            Height="38" 
                            Margin="5,0" 
                            Click="btnDelete_Click" 
                            Style="{StaticResource DangerButton}" 
                            FontSize="14"
                            Visibility="Collapsed"
                            ToolTip="Xóa quy tắc giá đã chọn"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>




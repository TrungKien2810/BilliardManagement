<UserControl x:Class="Billiards.UI.Views.LoyaltyManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource PrimaryColor}"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,0,0,20">
            <TextBlock Text="⭐ Quản lý Tích điểm Khách hàng Thân thiết" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Left"/>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Info Card -->
                <Border Background="White"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="ℹ️ Thông tin" 
                                   FontSize="14" 
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryColor}"
                                   Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" 
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondaryColor}"
                                   LineHeight="20">
                            <Run Text="Hệ thống tích điểm cho phép khách hàng tích điểm khi thanh toán và đổi điểm để được giảm giá."/>
                            <LineBreak/>
                            <Run Text="Quy tắc tích điểm: Số VND cần để có 1 điểm (ví dụ: 10.000đ = 1 điểm)"/>
                            <LineBreak/>
                            <Run Text="Quy tắc đổi điểm: 1 điểm = bao nhiêu VND khi giảm giá (ví dụ: 1 điểm = 100đ)"/>
                            <LineBreak/>
                            <Run Text="Số điểm tối thiểu: Số điểm tối thiểu để được phép đổi điểm (ví dụ: 1000 điểm)"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Form -->
                <Border Background="White"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="25">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="⚙️ Cấu hình Quy tắc Tích điểm"
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryColor}"
                                   Margin="0,0,0,20"/>

                        <StackPanel Grid.Row="1" Margin="0,0,0,15">
                            <TextBlock Text="Quy tắc tích điểm (VNĐ cần để có 1 điểm):" 
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>
                            <TextBox x:Name="txtPointsPerAmount" 
                                     Height="35"
                                     FontSize="14"
                                     Padding="10,8"
                                     TextChanged="txtPointsPerAmount_TextChanged"/>
                            <TextBlock Text="Ví dụ: 10000 = 10.000đ = 1 điểm" 
                                       FontSize="11" 
                                       Foreground="{StaticResource TextSecondaryColor}" 
                                       Margin="0,5,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Margin="0,0,0,15">
                            <TextBlock Text="Quy tắc đổi điểm (1 điểm = ? VNĐ giảm giá):" 
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>
                            <TextBox x:Name="txtAmountPerPoint" 
                                     Height="35"
                                     FontSize="14"
                                     Padding="10,8"
                                     TextChanged="txtAmountPerPoint_TextChanged"/>
                            <TextBlock Text="Ví dụ: 100 = 1 điểm = 100đ giảm giá" 
                                       FontSize="11" 
                                       Foreground="{StaticResource TextSecondaryColor}" 
                                       Margin="0,5,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="3" Margin="0,0,0,15">
                            <TextBlock Text="Số điểm tối thiểu để đổi điểm:" 
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>
                            <TextBox x:Name="txtMinimumPointsToRedeem" 
                                     Height="35"
                                     FontSize="14"
                                     Padding="10,8"
                                     TextChanged="txtMinimumPointsToRedeem_TextChanged"/>
                            <TextBlock Text="Ví dụ: 1000 = cần ít nhất 1000 điểm mới được đổi" 
                                       FontSize="11" 
                                       Foreground="{StaticResource TextSecondaryColor}" 
                                       Margin="0,5,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="4" Margin="0,0,0,20">
                            <CheckBox x:Name="chkIsActive"
                                      Content="Kích hoạt hệ thống tích điểm"
                                      FontSize="13"
                                      FontWeight="SemiBold"
                                      IsChecked="True"
                                      Margin="0,10,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="5" 
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Right">
                            <Button x:Name="btnSave" 
                                    Content="💾 Lưu" 
                                    Width="120" 
                                    Height="45" 
                                    Margin="5,0" 
                                    Click="btnSave_Click" 
                                    FontSize="14"
                                    FontWeight="Bold"
                                    ToolTip="Lưu cấu hình quy tắc tích điểm"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Example Card -->
                <Border Background="#E3F2FD"
                        BorderBrush="{StaticResource SecondaryColor}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="📊 Ví dụ" 
                                   FontSize="14" 
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SecondaryColor}"
                                   Margin="0,0,0,10"/>
                        <TextBlock x:Name="txtExample" 
                                   TextWrapping="Wrap" 
                                   FontSize="12"
                                   Foreground="{StaticResource TextColor}"
                                   LineHeight="20"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>


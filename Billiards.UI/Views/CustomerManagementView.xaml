<UserControl x:Class="Billiards.UI.Views.CustomerManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column: DataGrid -->
        <StackPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBlock Text="Danh sách Khách hàng" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryColor}"
                       Margin="0,0,0,15"/>
            <DataGrid x:Name="dgCustomers" 
                      AutoGenerateColumns="False"
                      SelectionChanged="dgCustomers_SelectionChanged"
                      IsReadOnly="True"
                      Height="500">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                    <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="SĐT" Binding="{Binding PhoneNumber}" Width="150"/>
                    <DataGridTextColumn Header="Điểm tích lũy" Binding="{Binding LoyaltyPoints}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <!-- Right Column: Form -->
        <StackPanel Grid.Column="1">
            <TextBlock Text="Thông tin Khách hàng" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryColor}"
                       Margin="0,0,0,15"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Họ tên:" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtFullName" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBlock Text="Số điện thoại:" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtPhoneNumber" Height="30" TextChanged="txtPhoneNumber_TextChanged"/>
                    <TextBlock x:Name="txtPhoneValidation" Margin="0,5,0,0" Foreground="OrangeRed"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <TextBlock Text="Điểm tích lũy:" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtLoyaltyPoints" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnAddNew" Content="Thêm mới" Width="110" Height="38" Margin="5,0" Click="btnAddNew_Click" FontSize="14"/>
                    <Button x:Name="btnSave" Content="Lưu" Width="110" Height="38" Margin="5,0" Click="btnSave_Click" FontSize="14"/>
                    <Button x:Name="btnDelete" Content="Xóa" Width="110" Height="38" Margin="5,0" Click="btnDelete_Click" Style="{StaticResource DangerButton}" FontSize="14"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>




<Window x:Class="Billiards.UI.Windows.InvoiceDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chi tiết Đơn hàng" 
        Height="750" 
        Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style x:Key="InfoCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InfoLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryColor}"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InfoValueStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="30,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" 
                        Background="{StaticResource PrimaryColor}"
                        CornerRadius="10,10,0,0"
                        Padding="25,20"
                        Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="📋" FontSize="32" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="CHI TIẾT ĐƠN HÀNG" 
                                           FontSize="24" 
                                           FontWeight="Bold"
                                           Foreground="White"/>
                                <TextBlock x:Name="txtInvoiceId" 
                                           FontSize="14"
                                           Foreground="White"
                                           Opacity="0.9"
                                           Margin="0,5,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <Border Grid.Column="1"
                                x:Name="borderStatus"
                                Background="White"
                                CornerRadius="20"
                                Padding="20,10"
                                VerticalAlignment="Center">
                            <TextBlock x:Name="txtStatus" 
                                       Text="Đã thanh toán"
                                       FontWeight="Bold"
                                       FontSize="14"
                                       Foreground="{StaticResource SuccessColor}"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Thông tin chung -->
                <Border Grid.Row="1" Style="{StaticResource InfoCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="ℹ️ THÔNG TIN CHUNG"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryColor}"
                                   Margin="0,0,0,15"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Column 1 -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="🎯 Bàn:" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtTable" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="⏰ Bắt đầu:" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtStartTime" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="⏱️ Kết thúc:" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtEndTime" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>

                            <!-- Column 2 -->
                            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="👤 Khách hàng:" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtCustomer" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="👨‍💼 Nhân viên:" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtEmployee" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Chi tiết sản phẩm -->
                <Border Grid.Row="2" Style="{StaticResource InfoCardStyle}">
                    <StackPanel>
                        <TextBlock Text="🍹 CHI TIẾT SẢN PHẨM" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryColor}"
                                   Margin="0,0,0,15"/>
                        <DataGrid x:Name="dgInvoiceDetails" 
                                  AutoGenerateColumns="False" 
                                  IsReadOnly="True" 
                                  CanUserAddRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  BorderThickness="0"
                                  RowBackground="White"
                                  AlternatingRowBackground="#F9F9F9"
                                  MinHeight="200">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="{StaticResource PrimaryLightColor}"/>
                                    <Setter Property="Foreground" Value="{StaticResource PrimaryDarkColor}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Padding" Value="12,10"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Sản phẩm" 
                                                    Binding="{Binding Product.ProductName}" 
                                                    Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="12,8"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Số lượng" 
                                                    Binding="{Binding Quantity}" 
                                                    Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="12,8"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Đơn giá" 
                                                    Binding="{Binding UnitPrice, StringFormat=\{0:N0\} VNĐ}" 
                                                    Width="140">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="12,8"/>
                                            <Setter Property="TextAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Thành tiền" 
                                                    Binding="{Binding Total, StringFormat=\{0:N0\} VNĐ}" 
                                                    Width="150">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="12,8"/>
                                            <Setter Property="TextAlignment" Value="Right"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="{StaticResource SecondaryColor}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Tóm tắt thanh toán -->
                <Border Grid.Row="3" 
                        Style="{StaticResource InfoCardStyle}"
                        Background="#E8F5E9">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="💰 TÓM TẮT THANH TOÁN"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SuccessColor}"
                                   Margin="0,0,0,15"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Tiền giờ -->
                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                       Text="Tổng tiền giờ:" 
                                       FontSize="14"
                                       Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" 
                                       x:Name="txtTableFee" 
                                       Text="0 VNĐ" 
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Right"
                                       Margin="0,0,0,10"/>

                            <!-- Tiền dịch vụ -->
                            <TextBlock Grid.Row="1" Grid.Column="0" 
                                       Text="Tổng tiền dịch vụ:" 
                                       FontSize="14"
                                       Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" 
                                       x:Name="txtProductFee" 
                                       Text="0 VNĐ" 
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Right"
                                       Margin="0,0,0,10"/>

                            <!-- Giảm giá -->
                            <TextBlock Grid.Row="2" Grid.Column="0" 
                                       Text="Giảm giá:" 
                                       FontSize="14"
                                       Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" 
                                       x:Name="txtDiscount" 
                                       Text="0 VNĐ" 
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource ErrorColor}"
                                       HorizontalAlignment="Right"
                                       Margin="0,0,0,10"/>

                            <!-- Separator -->
                            <Separator Grid.Row="3" Grid.ColumnSpan="2" 
                                       Background="{StaticResource BorderColor}"
                                       Margin="0,5"/>

                            <!-- Tổng -->
                            <TextBlock Grid.Row="4" Grid.Column="0" 
                                       Text="TỔNG THANH TOÁN:" 
                                       FontWeight="Bold" 
                                       FontSize="18"
                                       Foreground="{StaticResource SuccessColor}"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" 
                                       x:Name="txtTotalAmount" 
                                       Text="0 VNĐ" 
                                       FontWeight="Bold" 
                                       FontSize="20" 
                                       Foreground="{StaticResource SuccessColor}"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Close Button -->
                <Button Grid.Row="5" 
                        Content="✕ ĐÓNG" 
                        Click="btnClose_Click" 
                        Height="50"
                        FontSize="15"
                        FontWeight="Bold"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,10,0,0"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
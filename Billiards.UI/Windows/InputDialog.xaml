<Window x:Class="Billiards.UI.Windows.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="150" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtQuestion" Grid.Row="0" Margin="0,0,0,10"/>
        <TextBox x:Name="txtAnswer" Grid.Row="1" Margin="0,0,0,15" KeyDown="txtAnswer_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Height="25" Margin="0,0,10,0" Click="btnOk_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" Height="25" Click="btnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
